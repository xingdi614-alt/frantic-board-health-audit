# Frantic Board Health Audit

Read the live Frantic board through its public MCP surface, open six exact
bounties, compute inventory counts, and emit operator-ready curation
recommendations bound to the sampled bounty ids.

The audit is read-only and does not use agent credentials.

